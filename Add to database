# How to add something to database
1.) Create a  form with method post
2.) Create an input and set the name to "name"
3.) In this code, we don't even need the "id" you can ignore that
4.) Then we create another input type "submit" and set the name to "add"

<form action="<?php echo $_SERVER['PHP_SELF']; ?>" method="post">
 <input type="text" name="name" id="employee_name" autocomplete="off">
   <!-- Add more inputs if you want depending on your variable -->
 <input type="submit" name="add_employee" id="submit" value="Add Employee">
</form>

5.) After that, create a function
6.) Inside if statement, define your submit variable
7.) Create variables

function addFunction($conn){
 if ($_SERVER["REQUEST_METHOD"] == "POST" && isset($_POST['add'])) {
  $name = $_POST['name'];
	 if (empty($name)) {
            echo "Name field is empty. Please fill in the name.";
	}else {
	      $name = $conn->real_escape_string($name);
	      $check_sql = "SELECT * FROM (table name) WHERE name = '$name' AND user_id = $user_id";
	      $check_result = $conn->query($check_sql);
 	      $sql = "INSERT INTO (table name) (name, user_id)
                VALUES ('$name', '$user_id')";
	      if($conn->query($sql) === TRUE) {
		 echo "Employee added successfully";
	      }else{
		echo "Error"
	      }

	}
}

if ($_SERVER["REQUEST_METHOD"] == "POST" && isset($_POST['add'])) { addFunction($conn); }
